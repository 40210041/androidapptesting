<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!--maybe i should keep this proj-->
    <!--so with constraint layout, you need to specify the layout constraints of each component-->
    <!--each widget should have a app:layout_constrait something-->

    <EditText
        android:id="@+id/edt_1"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btn_1"
        app:layout_constraintHorizontal_bias="0.5"
        android:hint="@string/edit_msg"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        android:ems="10" />

    <Button
        android:id="@+id/btn_1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBaseline_toBaselineOf="@id/edt_1"
        app:layout_constraintStart_toEndOf="@id/edt_1"
        android:text="@string/btn_msg"
        android:onClick="send_message"/>


</android.support.constraint.ConstraintLayout>